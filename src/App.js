import Header from "./components/Header";

const App = () => {
  return (
    <div>
      {/* Header (will be sticky) */}
      <Header />
      {/* App Body 
        SideProfile
        Feed
      */}
    </div>
  );
};

export default App;
